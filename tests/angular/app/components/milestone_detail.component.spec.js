ngDescribe({
    name: 'Test milestone-detail component',
    modules: 'app',
    element: '<milestone-detail></milestone-detail>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
