ngDescribe({
    name: 'Test header component',
    modules: 'app',
    element: '<header></header>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
