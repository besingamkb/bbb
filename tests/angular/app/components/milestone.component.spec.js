ngDescribe({
    name: 'Test milestone component',
    modules: 'app',
    element: '<milestone></milestone>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
